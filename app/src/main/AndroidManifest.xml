<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.user.newsweats">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/newsappicon"
        tools:replace="android:icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Login"
            android:theme="@style/flash">

        </activity>
        <activity
            android:name=".UI.MainTabbedAct"
            android:label="Newsweat"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".UI.Registration" />
        <activity android:name=".UI.NewsDetailview" />
        <activity android:name=".UI.DetailImageview" />
        <activity android:name=".UI.ImagePagerAct" />
        <activity android:name=".UI.NewsWebview" />
        <activity android:name=".UI.InitialChecker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>